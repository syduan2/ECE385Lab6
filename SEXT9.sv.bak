module SEXT9 (input [8:0] IR_out,
					output [15:0] SEXT9_out);
					
	integer i;
			
	always_comb
	begin
		assign SEXT9_out[8:0] = IR_out[8:0];
		for (i = 9; i < 16; i = i + 1) // FOR LOOPS NOT WORKING WAT
		assign SEXT9_out[i] = IR_out[8];
	end
	
endmodule
